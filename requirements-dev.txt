# Dependencias solo de desarrollo/test -- NO se instalan en la imagen de
# producción (el Dockerfile solo instala requirements.txt). Instalar con:
#   pip install -r requirements.txt -r requirements-dev.txt
pytest==9.1.1
playwright==1.55.0
